services:
  api:
    build: .
    ports:
      - "8080:5000"
    env_file:
      - .env
    depends_on:
      - database

  database:
    image: postgres:15
    env_file:
      - .env
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
